<%= form_with(model: law_modification, local: true) do |form| %>
  <% if law_modification.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(law_modification.errors.count, "error") %> prohibited this law_modification from being saved:</h2>

      <ul>
        <% law_modification.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :law_id %>
    <%= form.number_field :law_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
