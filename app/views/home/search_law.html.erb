<% if @query.blank? %>
  <% content_for :title, "Búsqueda - TodoLegal" %>
<% else %>
  <% content_for :title, @query + " - TodoLegal" %>
<% end %>

<div class="container">

  <%= render partial: 'search_law_input', locals: {target_url: '/search_law', home_flag: false} %>

  <%= render 'search_result' %>

  <%= link_to 'Ir a inicio', root_path %>

</div>