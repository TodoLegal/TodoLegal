<%= form_with(model: document, local: true) do |form| %>
  <% if document.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(document.errors.count, "error") %> prohibited this document from being saved:</h2>

      <ul>
        <% document.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <br>

  <div class="field">
    <%= form.label :proceso_automático %>
    <%= form.select(:auto_process_type, options_for_select([['Ninguno', "none"], ['Procesar Gaceta', "process"], ['Procesar y partir Gaceta', "slice"], ['Subir Sentencia', "judgement"]], @selected_document_type)) %>
  </div>

  <div class="field">
    <%= form.file_field :original_file %>
  </div>

  <br>

  <div class="actions">
    <%= form.submit "Subir", class: 'btn btn-primary' %>
  </div>
<% end %>