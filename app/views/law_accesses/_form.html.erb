<%= form_with(model: law_access, local: true) do |form| %>
  <% if law_access.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(law_access.errors.count, "error") %> prohibited this law_access from being saved:</h2>

      <ul>
        <% law_access.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
